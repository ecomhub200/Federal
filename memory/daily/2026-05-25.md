# Daily Log: 2026-05-25

## Sessions

## Memory Maintenance

### Session (00:09)

**Context:** CC 309 (W1.2) — Scorecard module: fixing SC3 (5-yr trend always shows 0.0%) plus 4 UI enhancements across 4 sequential commits on branch `w1-2-scorecard-enhance`.

**Key Exchanges:**
- Live audit (Cowork 2026-05-24) confirmed SC1 and SC2 were stale — `scorecard.js` has zero `state === 'delaware'` branches and zero hardcoded "Delaware" UI strings. Only legitimate state literals are the 51-state population dict (~L106) and regional grouping array (~L629).
- SC3 root cause identified: `r.yoy_total` / `r.yoy_fatal` columns are NULL for non-federal tiers (those columns only exist on `federal_summary`), so the delta falls back to 0 → "▬ 0.0%". Bug site is approximately L591–597 in `scorecard.js`.
- Fix approach: derive delta from `r.spark_5yr` (the 5-year history array already fetched for sparklines, populated ~L417–420), avoiding any dependency on the absent `yoy_*` columns.

**Decisions Made:**
- SC1 and SC2 to be retired from `FROZEN_TABS.md` with an explanatory note; they were never real bugs.
- Four sequential PRs on one branch (`w1-2-scorecard-enhance`): SC3 fix → state-wide KPI band → KSI/100K legend + table density → interactive Metric Comparison chart.
- All new code must be state-agnostic (no hardcoded state names); KPI band aggregates client-side from already-loaded `scorecard_rankings` rows — no new backend queries.
- Per 100K toggle must be respected by the interactive chart (Step 4).
- No choropleth map, no side-by-side comparison drawer — both deferred.

**Lessons Learned:**
- `yoy_total` / `yoy_fatal` fields from `scorecard_rankings` are NULL for county/jurisdiction tiers; they exist only on the `federal_summary` matview. Never rely on those columns for per-jurisdiction delta math.
- The 5-year sparkline history (`spark_5yr`) is a reliable fallback for YoY delta: `spark_5yr[last] - spark_5yr[secondToLast]` gives the correct delta without an extra query.
- FROZEN_TABS bug reports can go stale after module refactors — always do a live grep audit before coding a fix.

**Action Items:**
- Complete Step 1 (SC3 delta fix) using `spark_5yr` array; verify non-zero deltas + green/red colors + `—` for missing prior-year data before proceeding.
- Steps 2–4 gated on Cowork live-verify sign-off after each PR merge.
- Update `FROZEN_TABS.md` (Step 5) to retire SC1/SC2 and mark SC3 fixed.
- Next milestone after W1.2: W1.3 (county → Planning District rollup audit).

