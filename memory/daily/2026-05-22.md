# Daily Log: 2026-05-22

## Sessions

## Memory Maintenance

### Session (01:16)

**Context:** P1 regression fix — Dashboard tab tier KPI buttons all showed identical values (569,829 Delaware-wide crashes) regardless of tier selection.

**Key Exchanges:**
- Bug: All 7 Dashboard tier buttons (Federal/State/Region/MPO/PD/County/City) showed the same KPI value (569,829) instead of tier-appropriate aggregates
- Map tab's tier switching *works* and was identified as the reference pattern to model the fix after
- Investigation traced through `dashboard/dashboard-tab-matview.js`, `dashboard/dashboard-tab-kpi.js`, `modules/data/supabase-bridge`, and `handleTierChange` as the likely fix locations
- Key question: Does `getSummary` / the dashboard KPI fetch omit the `tier` parameter, causing it to always return the state-wide aggregate?
- Branch target: `claude/fix-dashboard-tier-kpi-v2` (push only, no PR)

**Decisions Made:**
- Re-do of CC-011 (which was never attempted) with a more prescriptive prompt including explicit ABORT conditions and required tier-sweep verification
- Federal tier must omit the `state=eq.X` filter so it aggregates across all states; other tiers must pass tier + value to the query/RPC
- Fix should subscribe Dashboard KPI fetcher to the same tier-change event used by the Map tab

**Lessons Learned:**
- CC-011 was "vague enough that CC never even attempted it" — original attempt produced no branch on origin; explicit probes, code patterns to model after, and ABORT conditions are needed for complex bugs
- `handleTierChange` calls `injectFastDashboard` for federal/state tiers, but the underlying data fetch apparently ignores the tier parameter — root cause likely in `getSummary` or the matview query builder
- The `[[INDEX_MAP]]` line-number ranges are stale (built from 159,387-line snapshot vs live ≈153,085 lines); always re-derive anchors by function name, never by snapshot line range

**Action Items:**
- Verify the fix makes Federal >> State >> County >> City (tier sweep with screenshots for each tier)
- Confirm `node --check` passes on touched files and `git diff --stat` shows only dashboard modules + `app/CODE_MAP.md`
- Update `app/CODE_MAP.md` for any line-number shifts in dashboard modules
- No PR needed — push branch `claude/fix-dashboard-tier-kpi-v2` only

### Session (03:11)

**Context:** Working on Upload tab Phase 1 certification fixes — four issues (F1–F4) around tier auto-select, state default, localStorage cleanup, and `jurisdictionChanged` debounce.

**Key Exchanges:**
- Explored `app/modules/spatial/geo-tier.js` to map tier-button architecture: 7 tier buttons (`handleTierChange(tier)` onclick), each tier has its own dropdown (`tierRegionSelect`, `tierMPOSelect`, `tierPlanningDistrictSelect`, `tierCountySelect`, `tierCitySelect`) and a corresponding selection handler (`handleRegionSelection()`, etc.)
- `populateGeoTierDropdown()` is at `geo-tier.js:246–339`; Region/MPO/PD dropdowns populated at lines 783–817
- Backend verification probe: `dashboard_summary?state=eq.delaware&dot_district=eq.North+District` must return non-zero rows before F1 is meaningful

**Decisions Made:**
- F1 (auto-select): After `handleTierChange()` shows the relevant dropdown row, auto-select first non-empty option and fire the tier's handler — each tier has its own handler, no generic solution
- F2 (default state): Read `localStorage.getItem('crashlens.lastSelectedState')` instead of hardcoded Virginia FIPS `51` — exact location `upload-tab.js:476–477`
- F3 (localStorage cleanup): Whitelist approach — only clear state-scoped keys on state change; must NOT clear `crashlens.lastSelectedState`, `crashlens.appearance.*`, or auth tokens; must build whitelist from real grep of `localStorage.getItem/setItem/removeItem` calls
- F4 (debounce): Add `fireJurisdictionChanged(detail)` wrapper with 250 ms debounce at the dispatch source in Upload modules; replace all `document.dispatchEvent(new CustomEvent('jurisdictionChanged', ...))` calls; do NOT debounce at listener side

**Lessons Learned:**
- `jurisdictionChanged` fires 4–7× per single tier-button click (Chrome MCP console log evidence); only `mapPointsHydrate` has its own dedup guard; all other listeners (Schools, Transit, BTS, Overture, Scorecard) waste cycles on every fire
- Region/PD/MPO tier handlers use `HierarchyRegistry.getData()` / `.getRegions()` / `.getMPOs()` / `.getPlanningDistricts()` for hierarchy resolution
- All changes must be confined to `app/modules/upload/` — no shared modules, no state literals added

**Action Items:**
- Grep real localStorage keys from Upload modules + `assets/js/` before building F3 whitelist
- Grep all `jurisdictionChanged` dispatch sites before implementing F4 wrapper
- Run backend curl probe for Delaware North District — abort if 0 rows
- After merge: Murad to run 3 paste-and-run JS snippets via Chrome MCP to verify F1 banner, F3 cross-state isolation, F4 fire count === 1
- Branch: `claude/upload-phase1-fixes`; push triggers Phase 1 re-certification; if green, Upload tab added to `FROZEN_TABS.md`

