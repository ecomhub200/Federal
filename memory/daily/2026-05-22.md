# Daily Log: 2026-05-22

## Sessions

## Memory Maintenance

### Session (01:16)

**Context:** P1 regression fix — Dashboard tab tier KPI buttons all showed identical values (569,829 Delaware-wide crashes) regardless of tier selection.

**Key Exchanges:**
- Bug: All 7 Dashboard tier buttons (Federal/State/Region/MPO/PD/County/City) showed the same KPI value (569,829) instead of tier-appropriate aggregates
- Map tab's tier switching *works* and was identified as the reference pattern to model the fix after
- Investigation traced through `dashboard/dashboard-tab-matview.js`, `dashboard/dashboard-tab-kpi.js`, `modules/data/supabase-bridge`, and `handleTierChange` as the likely fix locations
- Key question: Does `getSummary` / the dashboard KPI fetch omit the `tier` parameter, causing it to always return the state-wide aggregate?
- Branch target: `claude/fix-dashboard-tier-kpi-v2` (push only, no PR)

**Decisions Made:**
- Re-do of CC-011 (which was never attempted) with a more prescriptive prompt including explicit ABORT conditions and required tier-sweep verification
- Federal tier must omit the `state=eq.X` filter so it aggregates across all states; other tiers must pass tier + value to the query/RPC
- Fix should subscribe Dashboard KPI fetcher to the same tier-change event used by the Map tab

**Lessons Learned:**
- CC-011 was "vague enough that CC never even attempted it" — original attempt produced no branch on origin; explicit probes, code patterns to model after, and ABORT conditions are needed for complex bugs
- `handleTierChange` calls `injectFastDashboard` for federal/state tiers, but the underlying data fetch apparently ignores the tier parameter — root cause likely in `getSummary` or the matview query builder
- The `[[INDEX_MAP]]` line-number ranges are stale (built from 159,387-line snapshot vs live ≈153,085 lines); always re-derive anchors by function name, never by snapshot line range

**Action Items:**
- Verify the fix makes Federal >> State >> County >> City (tier sweep with screenshots for each tier)
- Confirm `node --check` passes on touched files and `git diff --stat` shows only dashboard modules + `app/CODE_MAP.md`
- Update `app/CODE_MAP.md` for any line-number shifts in dashboard modules
- No PR needed — push branch `claude/fix-dashboard-tier-kpi-v2` only

