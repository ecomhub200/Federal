# Daily Log: 2026-04-19

## Sessions

### Session (12:20) - Wiki Deployment & Initial Seeding

**Context:** User asked Claude to deploy the Karpathy-style LLM wiki
(https://gist.github.com/karpathy/442a6bf555914893e9891c11519de94f) and keep
CLAUDE.md in sync. The wiki scaffolding (`memory/AGENTS.md`, hooks, scripts)
already existed but runtime directories were missing, and the project's root
CLAUDE.md had no reference to the wiki system.

**Key Exchanges:**
- Confirmed `.claude/settings.json` already wires SessionStart / SessionEnd /
  PreCompact to `memory/hooks/*.py`.
- SessionStart hook was verified to emit the expected
  `hookSpecificOutput.additionalContext` JSON with the (empty) index.
- User then asked to start seeding the wiki. Decided to lift structured
  knowledge out of `CLAUDE.md` rather than wait for organic conversations,
  because CLAUDE.md already contains canonical, encyclopedia-ready material.

**Decisions Made:**
- **Wiki lives in `memory/`**, hooks fire automatically inside Claude Code,
  compile/query/lint run via `uv` against `memory/pyproject.toml`.
- **Seed strategy**: extract atomic concepts from CLAUDE.md (state
  management, EPDO weights, DOT-neutral naming, modular architecture) as the
  first wave. Future daily logs will grow the graph organically.
- **Branch**: all wiki work lands on `claude/deploy-wiki-update-docs-TPKvS`,
  no direct pushes to main (per CLAUDE.md rule #1).
- **Naming**: lowercase-kebab filenames, Obsidian-style `[[wikilinks]]`
  relative to `knowledge/`, YAML frontmatter mandatory.

**Lessons Learned:**
- `memory/.gitignore` tracks `daily/` and `knowledge/` intentionally — they
  ARE the knowledge base and must be versioned.
- `reports/` is gitignored; `lint.py` regenerates it.
- The SessionStart hook truncates at 20,000 chars; keep `index.md` compact
  (summaries, not full articles) so it stays under budget as the KB grows.
- `scripts/config.py` names the canonical sub-dirs: `concepts/`,
  `connections/`, `qa/`. Any new article directory must be added here and in
  `utils.list_wiki_articles()`.

**Action Items:**
- [x] Seed `concepts/state-management.md` from CLAUDE.md "State Management"
- [x] Seed `concepts/epdo-weights.md` from CLAUDE.md "EPDO Calculation"
- [x] Seed `concepts/dot-neutral-naming.md` from CLAUDE.md "DOT-Neutral
      Column Naming Convention"
- [x] Seed `concepts/module-architecture.md` from CLAUDE.md "Modular
      Architecture" + "Module Conventions"
- [x] Update `knowledge/index.md` and `knowledge/log.md` to register the seeds
- [x] Add `concepts/stripe-payment-flow.md`,
      `concepts/ai-context-awareness.md`, `concepts/state-onboarding.md`,
      `concepts/coolify-deployment.md`, and
      `connections/state-scope-and-ai-context.md`

### Session (12:55) - Batch 2 Seed

**Context:** User said "keep going" after reviewing the first four seed
articles. Wrote the four remaining concept articles from CLAUDE.md's
high-value sections (Payment Architecture, AI Tab Context Awareness,
Multi-State Data Onboarding, Hosting: Coolify) and the first connection
article tying state management to the AI resolver.

**Key Exchanges:**
- Decided to write a dedicated `connections/` article rather than bury
  the insight inside `state-management` or `ai-context-awareness`. The
  cross-tab-selection-plus-indicator contract is important enough to
  stand alone.
- Kept scope discipline: each concept covers one system; connections
  cover relationships across systems.

**Decisions Made:**
- `stripe-payment-flow` explicitly warns against trusting the success
  redirect — the webhook is the source of truth.
- `coolify-deployment` calls out the secret/public split that
  `entrypoint.sh` enforces, because leaking `STRIPE_SECRET_KEY` into
  `api-keys.json` would be a production incident.
- `state-onboarding` enumerates the 10-step checklist and the required
  onboarding-doc sections verbatim from CLAUDE.md so that the doc is
  self-contained (no need to cross-reference CLAUDE.md at onboarding
  time).

**Lessons Learned:**
- The structural linter's backlinks check quickly surfaces one-way
  wikilinks; adding the missing reverse links is almost always correct.
- Writing a `connections/` article forces clarity about why two
  concepts belong to the same mental model. The prose tends to be
  higher-value than another concept page.

**Next Action Items:**
- [ ] After the next real coding session, compile.py (or another manual
      seed) should extract any architectural decisions that weren't in
      CLAUDE.md (e.g. nuances of `updateAIContextIndicator()` timing,
      debug helpers actually present in code).
- [ ] Consider a `concepts/grants-ranking.md`, `concepts/warrants-
      analysis.md`, and `concepts/batch-before-after.md` to cover the
      remaining tabs.
- [ ] Add a `concepts/firebase-auth.md` covering Google OAuth + Email/
      Password + the `CrashLensAuth` module surface.
